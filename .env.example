# Keycloak Configuration (Production)
VITE_KEYCLOAK_URL=https://keycloak-production-7ec8.up.railway.app
VITE_KEYCLOAK_REALM=deepskyn
VITE_KEYCLOAK_CLIENT_ID=frontend

# Backend API URL (Production)
VITE_API_URL=https://your-backend.example.com

# Note: NEVER include DATABASE_URL or backend secrets in frontend .env
# Backend secrets (DATABASE_URL, KEYCLOAK_CLIENT_SECRET) must be set on Railway only
